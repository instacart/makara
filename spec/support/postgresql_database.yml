test:
  adapter: 'postgresql_makara'
  database: 'makara_test'

  timeout: 5000

  makara:
    blacklist_duration: 2
    primary_ttl: 5
    connections:
      - role: primary
      - role: replica
      - role: replica
