test:
  adapter: 'postgresql_makara'
  database: 'makara_test'

  timeout: 5000

  makara:
    blacklist_duration: 2
    master_ttl: 5
    connections:
      - role: master
      - role: replica
      - role: replica
